# v1.5 변경 사항

## 정보 구조

- 국가공무원, 한국사능력검정시험, Q-Net 독립 안내 페이지 추가
- 각 페이지에 공식 규격, 자동 확인, 직접 확인, 사용 순서, FAQ 추가
- 안내 페이지 CTA에서 해당 프리셋을 자동 선택하도록 `?preset=` 지원

## 운영 페이지

- `contact.html`: 공식 규격 오류와 기능 제안 안내
- `changelog.html`: 버전별 변경 및 공식 출처 확인 기록
- GitHub Issue Form 2종
- `404.html`, `robots.txt`, `sitemap.xml`

## AdSense 인증 준비

- 모든 HTML에 빌드 토큰 `__ADSENSE_META__` 추가
- `ADSENSE_PUBLISHER_ID`가 있을 때만 공식 인증 메타태그 삽입
- 게시자 ID가 있을 때 루트 `ads.txt` 자동 생성
- 실제 광고 스크립트나 광고 영역은 아직 추가하지 않음

## 배포

- `scripts/build-site.mjs`를 단일 정적 빌드 경로로 사용
- GitHub Actions도 `npm run build` 사용
- Cloudflare Pages와 Netlify에서 같은 빌드 명령 사용 가능
- `_headers` 보안 헤더 파일과 `netlify.toml` 추가

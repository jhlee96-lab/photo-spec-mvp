{
  "name": "photo-spec-mvp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-only application photo crop, resize and compression tool",
  "scripts": {
    "check": "node --check app.js && node --check src/core.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

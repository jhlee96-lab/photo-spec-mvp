{
  "name": "photo-spec-mvp",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Official-source application photo crop, resize and browser-only validation tool with exam guide pages and deployment-time AdSense verification support",
  "scripts": {
    "check": "node --check app.js && node --check src/core.js && node --check scripts/build-site.mjs",
    "build": "node scripts/build-site.mjs",
    "test": "node --test --test-concurrency=1"
  },
  "engines": {
    "node": ">=20"
  }
}

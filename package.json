{
  "name": "photo-spec-mvp",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "description": "Official-source application photo crop, resize and browser-only validation tool with source-resolution warnings and portrait guides",
  "scripts": {
    "check": "node --check app.js && node --check src/core.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

{
  "name": "photo-spec-mvp",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Official-source application photo crop, resize and browser-only validation tool",
  "scripts": {
    "check": "node --check app.js && node --check src/core.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

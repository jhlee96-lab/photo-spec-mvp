# v1.3 변경 사항

## 화면 구성

- 상단 오른쪽의 현재 프리셋 요약 박스를 제거했습니다.
- 제목, 설명, CTA를 가운데 정렬했습니다.
- 프리셋 정보는 작업공간의 `01 규격 선택` 카드에서만 상세하게 보여 중복을 줄였습니다.
- 상단에는 원본 미전송, 공식 출처 표시, 새로고침 초기화만 짧게 안내합니다.

## Q-Net 국가자격 프리셋

공식 Q-Net 사진등록 안내에 따라 다음 조건을 반영했습니다.

- 크기: 300 × 400px 이상
- 형식: JPEG 또는 JPG
- 도구 출력: 300 × 400px JPG
- 공식 공통 최대 용량: 별도 안내 없음

공식 최소값을 출력하므로 결과 파일은 `qnet-300x400.jpg`입니다.

## 검사 로직

- 기존 프리셋: 지정된 출력 크기와 정확히 일치하는지 검사
- Q-Net: 가로 300px 이상, 세로 400px 이상인지 검사
- Q-Net은 공식 형식 표기를 `JPEG 또는 JPG`로 표시
- 용량 제한이 공식 공통 안내에 없으면 합격·불합격 대신 `안내`로 표시

## 데이터 처리

- 서비스 워커 없음
- 쿠키 없음
- Local Storage / Session Storage / IndexedDB 없음
- 사진 네트워크 전송 없음
